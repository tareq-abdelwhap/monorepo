{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "prepare:starter": "pnpm -F nuxt-starter dev:prepare",
    "dev:doti": "pnpm --filter doti-pad dev",
    "changeset": "changeset",
    "ci:version": "pnpm changelog && pnpx changeset version && pnpm install --no-frozen-lockfile && git add .",
    "ci:publish": "pnpm publish -r --no-git-checks && pnpm changeset tag",
    "changeset-publish": "changeset publish",
    "changeset-tag": "changeset tag"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.5.2",
  "devDependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
