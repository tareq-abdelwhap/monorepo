{
  "name": "new-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "netlify": "npx netlify deploy --dir dist --site ${{ secrets[matrix.app.site_id_secret] }} --auth ${{ secrets.NETLIFY_AUTH_TOKEN }}"
  },
  "dependencies": {
    "@nuxt/icon": "1.15.0",
    "@vueuse/nuxt": "13.4.0",
    "nuxt": "^3.17.5",
    "nuxt-starter": "workspace:*",
    "nuxt-swiper": "2.0.1",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "netlify": "^22.2.2"
  }
}
